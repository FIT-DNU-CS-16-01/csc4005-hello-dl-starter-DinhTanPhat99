Đã thiết lập môi trường Python riêng cho môn CSC4005 và cài đặt các thư viện từ `requirements.txt`.
Đã chạy kiểm tra môi trường bằng `python check_env.py` và kiểm tra khói bằng `python run_smoke_test.py`.
Cả hai lệnh đều hoạt động bình thường, tạo ra `outputs/logs/env_check.txt` và `outputs/logs/smoke_test_log.txt`.
Không gặp lỗi cài đặt lớn, chỉ cần đảm bảo dùng phiên bản Python tương thích và cài đầy đủ gói.
Nếu có sự cố, tôi sẽ kiểm tra lại `requirements.txt` và cài đặt lại môi trường ảo.
Tệp báo cáo này tóm tắt nhanh các bước đã thực hiện và kết quả chạy thành công.